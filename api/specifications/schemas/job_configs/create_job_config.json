{
  "title": "Create job config",
  "description": "Create a job config object",
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "required": [
    "config_name"
  ],
  "properties": {
    "config_name": {
        "type": "string",
        "minLength": 4
      }
    }
}
